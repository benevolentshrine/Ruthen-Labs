# Test Data

This is a rigorously structured directory to test the Yomi Indexer.
It contains nested directories, ignored files, hidden files, and empty files.

**UPDATE:** Adding a line to trigger the file watcher for incremental sync!
